# Pinned runtime dependencies baked into the training container image
# (training/Containerfile). This is the ONLY dependency set that matters for
# reproducibility of the actual TrainJob -- requirements-dev.txt is local-tooling only.

torch==2.4.1
numpy==1.26.4

# Optional experiment tracking client. train.py/evaluate.py degrade gracefully if
# MLFLOW_TRACKING_URI is unset, even though this package is always present in the image.
mlflow==2.16.2

# Optional S3-compatible checkpoint upload (see train.py CheckpointStore). Degrades
# gracefully if CHECKPOINT_S3_BUCKET is unset, even though the package is always present.
boto3==1.35.36
